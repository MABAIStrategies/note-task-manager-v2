# Deployment

Recommended target: Vercel for the web app and a serverless API or small Node service for backend routes.

For MVP demo:

```bash
npm install
npm run build
npm run preview
```

Before production, add real environment variables, auth, database migrations, and integration consent screens.
